# Production Environment Template
# FlowBiz Client: dhamma-channel-automation
#
# Usage:
#   1. Copy this file to .env in project root
#   2. Replace all <PLACEHOLDER> values
#   3. Keep .env secure (never commit to repo)
#
# This file is for VPS production deployment only.

# ============================================================
# FlowBiz Standard Variables (Required)
# ============================================================

# Port allocation (from config/flowbiz_port.env)
# This overrides for docker compose if not using --env-file
FLOWBIZ_ALLOCATED_PORT=3007

# Service identity
APP_SERVICE_NAME=dhamma-automation
APP_ENV=production

# Logging
APP_LOG_LEVEL=INFO

# CORS - Replace with your actual domain (e.g., https://dhamma.example.com)
APP_CORS_ORIGINS=https://<YOUR_DOMAIN.COM>

# Version tracking
FLOWBIZ_VERSION=<VERSION>
FLOWBIZ_BUILD_SHA=<GIT_SHA>

# ============================================================
# Application Variables
# ============================================================

# Legacy compatibility
APP_NAME=dhamma-automation

# Security - CHANGE THESE!
SECRET_KEY=<YOUR_SECRET_KEY>
ADMIN_USERNAME=<YOUR_ADMIN_USERNAME>
ADMIN_PASSWORD=<YOUR_ADMIN_PASSWORD>

# Paths (usually keep defaults)
OUTPUT_DIR=/app/output
DATA_DIR=/app/data

# ============================================================
# Soft-Live Configuration
# ============================================================

# Enable soft-live mode (true/false)
# When true, YouTube uploads follow SOFT_LIVE_YOUTUBE_MODE
SOFT_LIVE_ENABLED=true

# YouTube upload mode:
#   dry_run  - No actual upload, fake video ID returned
#   unlisted - Upload as unlisted video
#   public   - Upload as public (DANGER: production only)
SOFT_LIVE_YOUTUBE_MODE=dry_run

# Fail-closed behavior (true/false)
# When true, invalid config causes failure instead of fallback
SOFT_LIVE_FAIL_CLOSED=true

# ============================================================
# API Keys (Add only what you need)
# ============================================================

# OpenAI
# OPENAI_API_KEY=<YOUR_OPENAI_KEY>

# Google Cloud (for TTS)
# GOOGLE_APPLICATION_CREDENTIALS=/app/credentials.json

# YouTube API credentials are stored separately
# See: GOOGLE_CREDENTIALS_SETUP.md
